# Frontend Environment Variables (Local Development Only)
#
# Note: Most environment variables are in the ROOT .env file.
# This file contains ONLY frontend-specific VITE_* variables for local dev.
# In production, these are passed via docker-compose build-args.

# API URL for local development
VITE_API_URL=http://localhost:8000/v1

# Sentry DSN for frontend error tracking
# Get from https://sentry.serpdev.ru - leave empty to disable
VITE_SENTRY_DSN=
