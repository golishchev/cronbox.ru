[project]
name = "cronbox-ru"
version = "0.1.0"
description = "CronBox - HTTP request scheduling service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "backend",
]
